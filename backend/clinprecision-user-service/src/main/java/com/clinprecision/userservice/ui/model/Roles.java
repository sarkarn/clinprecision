package com.clinprecision.userservice.ui.model;

/**
 * Enum defining all available roles in the system.
 * This ensures consistency when referencing roles throughout the application.
 */
public enum Roles {
	USER,
	ADMIN,
	SYSTEM_ADMIN,
	DB_ADMIN,
	SPONSOR_ADMIN
}
